<div id="<%= dom_id publication %>">
  <p>
    <strong>Title:</strong>
    <%= publication.title %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= publication.content %>
  </p>

  <p>
    <strong>Tag:</strong>
    <%= publication.tag_id %>
    <%# <% @tags.each do |t| %>
        <%# <%= t.nombre if publication.tag_id == t.id %> 
    <%# <% end %> 
  </p>

  <p>
    <strong>User:</strong>
    <%= publication.user_id %>
  </p>

</div>
